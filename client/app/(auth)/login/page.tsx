import { LoginForm } from "@/components/auth/LoginForm";

export default function LoginPage() {
  return (
    <div className="flex-1 flex items-center justify-center p-4 w-full h-full animate-in fade-in duration-500">
      <LoginForm />
    </div>
  );
}
